import MovieList from './features/MovieList/MovieList';
import SearchContainer from './features/SearchContainer/SearchContainer';

function App() {
  return (
    <div>
      <SearchContainer/>
      <MovieList/>
    </div>
  );
}

export default App;
